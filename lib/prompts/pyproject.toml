[project]
name = "prompts"
version = "0.1.0"
description = "XMS - Prompt storage and versioning for actBI agents"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
