/**
 * Jest environment setup.
 * NODE_ENV is automatically set to "test" by Jest.
 * Routes use in-memory stores when NODE_ENV === "test".
 */

// No environment variables needed - tests use in-memory stores
